version: "3.8"
services:
  web:
    build: ./apps/web
    ports:
      - "3000:3000"
    env_file: .env

  api:
    build: ./apps/api
    ports:
      - "3001:3001"
    env_file: .env
    depends_on:
      - redis

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
